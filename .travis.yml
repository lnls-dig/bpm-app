# Travis CI script

language: c
compiler:
- clang
- gcc

sudo: required

env:
- ROLE=client BOARD=ml605
- ROLE=client BOARD=afcv3
- ROLE=server BOARD=ml605
- ROLE=server BOARD=afcv3
- ROLE=gateware

addons:
  apt:
    packages:
    - uuid-dev

before_install:
- git submodule update --init --recursive

#   Build and check this project
script:
- ./ci_build.sh
